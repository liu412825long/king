server:
  port: 8080
spring:
  application:
    name: king
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/king?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: arongking

mybatis:
  #指定xml所在的位置
  mapper-locations: classpath:mapper/*Mapper.xml
  #指定entity所在的位置
  type-aliases-package: cn.bj.king.entity


logging.file: /logs/${spring.application.name}.log
logging:
  pattern:
    file: '%d{yyyy-MMM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{15} - %msg%n'
  file:
    max-size: 50MB
    max-history: 3

